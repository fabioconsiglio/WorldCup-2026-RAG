# Environment
.env
.venv


# ChromaDB local data
local_db/

# Python
__pycache__/
*.pyc

# ETL state file (varies per machine)
etl_state.json

# Backups (optional — comment out if you want to track them)
database_backup.json